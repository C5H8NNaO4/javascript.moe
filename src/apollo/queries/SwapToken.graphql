mutation swapToken($provider: Providers!, $providerToken: OAuthCode!, $redirectUri: String!) {
  swapToken(provider: $provider, providerToken: $providerToken, redirectUri: $redirectUri) {
    signed
  }
}
